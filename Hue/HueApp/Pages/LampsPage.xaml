<Page
    x:Class="HueApp.Pages.LampsPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:HueApp.Pages"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    xmlns:data="using:Hue">

    <Grid Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">
        <Grid.RowDefinitions>
            <RowDefinition Height="auto"/>
            <RowDefinition Height="20"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <GridView ItemsSource="{x:Bind Lamps}" Margin="3,0,0,0">
            <GridView.ItemTemplate>
                <DataTemplate x:DataType="data:Lamp">
                    <Button Name="{x:Bind Number}" Content="{x:Bind Name}" Width="90" Height="90" Click="LampButton_Click"/>
                </DataTemplate>
            </GridView.ItemTemplate>
        </GridView>
        <GridView Margin="3,0,0,0" Grid.Row="2">
            <Button Content="All lamps" Width="90" Height="90"/>
        </GridView>
    </Grid>
</Page>
