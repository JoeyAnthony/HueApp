<Page
    x:Class="Hue.MainPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Hue"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Grid Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">
        <SplitView Name="Split" CompactPaneLength="0" OpenPaneLength="200" IsPaneOpen="False" DisplayMode="CompactOverlay">
            <SplitView.Pane>
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="auto"></RowDefinition>
                        <RowDefinition></RowDefinition>
                    </Grid.RowDefinitions>


                    <StackPanel Name="UpperControls" >
                        <TextBlock Height="40" FontSize="25" TextAlignment="Center">UpperButtons</TextBlock>
                        <Button Content="All Lamps" FontSize="20" HorizontalAlignment="Stretch"></Button>
                        <Button Content="Settings" FontSize="20" HorizontalAlignment="Stretch"></Button>
                        <Button Content="New Lamp" FontSize="20" HorizontalAlignment="Stretch" Click="Button_Click"></Button>
                        <TextBlock Height="40" FontSize="25" TextAlignment="Center">Lamps</TextBlock>
                    </StackPanel>

                    <ItemsControl Grid.Row="1" ItemsSource="{x:Bind Lamps}">
                        <ItemsControl.ItemsPanel>
                            <ItemsPanelTemplate>
                                <StackPanel></StackPanel>
                            </ItemsPanelTemplate>
                        </ItemsControl.ItemsPanel>
                        
                        <ItemsControl.ItemTemplate>
                            <DataTemplate x:DataType="local:Lamp">
                                <Button FontSize="20" HorizontalAlignment="Stretch" Content ="{x:Bind Name}"></Button>
                            </DataTemplate>
                        </ItemsControl.ItemTemplate>
                    </ItemsControl>

                </Grid>
            </SplitView.Pane>
            <SplitView.Content>
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="50"></RowDefinition>
                        <RowDefinition></RowDefinition>
                    </Grid.RowDefinitions>
                    <Grid Grid.Row="0">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition></ColumnDefinition>
                            <ColumnDefinition Width="auto"></ColumnDefinition>
                        </Grid.ColumnDefinitions>
                        <TextBlock Grid.Column="0" FontSize="30" TextAlignment="Left" Text="[LampName]"></TextBlock>
                        <Button Grid.Column="1" Name="SplitButton" Width="50" Height="50" HorizontalAlignment="Left" VerticalAlignment="Top" FontFamily="Segoe MDL2 Assets" FontSize="20" Content="&#xE700;" Click="OpenClosePane_Click"></Button>
                    </Grid>
                   
                </Grid>
            </SplitView.Content>
        </SplitView>
    </Grid>
</Page>
